import { describe, it, expect } from 'vitest';

// Spec: email-read — Requirement: Read Email
// Tests written FIRST (spec-driven). Implementation pending.

describe('email-read/Read Email', () => {
  it('Scenario: Read email with body and metadata', async () => {
    // WHEN read_email is called with {id: "msg123"}
    // THEN returns full email body as token-efficient markdown,
    //      sender, recipients, subject, timestamp, and attachment list
    expect.fail('Not implemented — awaiting read_email action');
  });
});
