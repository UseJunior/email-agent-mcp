import { describe, it, expect } from 'vitest';

// Spec: email-read — Requirement: Search Emails
// Tests written FIRST (spec-driven). Implementation pending.

describe('email-read/Search Emails', () => {
  it('Scenario: Search across all mailboxes', async () => {
    // WHEN search_emails is called with {query: "contract review", mailbox: null}
    // THEN searches across all configured mailboxes
    // AND returns matching emails with the originating mailbox name
    expect.fail('Not implemented — awaiting search_emails action');
  });
});
