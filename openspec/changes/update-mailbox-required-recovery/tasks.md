- [ ] Extend the `ActionError` shape in `packages/email-core/src/actions/compose-helpers.ts` with optional `availableMailboxes?: string[]` and `defaultMailbox?: string`
- [ ] Populate both fields in `checkMailboxRequired` from `allMailboxes` (`name`, and the entry with `isDefault: true`) and flip `recoverable` to `true`; leave `code` and the message string unchanged
- [ ] Extend all four error Zod schemas so the new fields survive output typing: `SendEmailOutput` (`send.ts:39`), `ReplyToEmailOutput` (`reply.ts:37`), `DraftOutput` (`draft.ts:25`), `SendDraftOutput` (`draft.ts:182`). Note MCP does not currently call `action.output.parse`, so extra fields survive transport today and plain `z.object` parsing strips rather than rejects them — the extension is for truthful inferred/public output types and future validation
- [ ] Add core tests covering enumeration (both names, no ordering assertion), the recoverable flag, and the named default (`describe('email-write/Recoverable Mailbox-Required Error')`)
- [ ] Add a round-trip test asserting a name taken from the error payload is accepted by a retry (`describe('mailbox-config/Mailbox Names Are Round-Trippable Selectors')`). Do NOT route the round trip through `list_mailboxes` — `listMailboxesAction` exists in core but `buildLazyActions()` registers no production MCP `list_mailboxes` tool (that is issue #93)
- [ ] Confirm the multi-mailbox MCP path surfaces the new fields end-to-end in `packages/email-mcp/src/server.test.ts` (the action context at `server.ts:721` already carries `allMailboxes`, built from connected mailboxes at `server.ts:468`)
- [ ] Run `openspec validate update-mailbox-required-recovery --strict`
- [ ] Run `npm run test:run --workspaces`, `npm run lint --workspaces`, and `npm run check:spec-coverage`
