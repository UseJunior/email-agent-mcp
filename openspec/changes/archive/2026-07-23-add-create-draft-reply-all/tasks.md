- [x] Add `reply_all: z.boolean().optional().default(true)` to `CreateDraftInput` in `packages/email-core/src/actions/draft.ts`, with a `.describe()` mirroring `reply.ts:27` and noting it only applies when `reply_to` is set
- [x] Pass `replyAll: input.reply_all` to `createReplyDraft` in the `replyTo` branch (call at `draft.ts:135`, `replyAll` key at `:139`). Kept the raw `input.reply_all` forward (not `!== false`) to match the sibling reply-draft path in `reply.ts:173`; the provider contract treats `undefined` as reply-all, so an unparsed direct `.run()` call still defaults correctly.
- [x] Mention the flag in the `create_draft` action description so it surfaces in the MCP tool listing
- [x] Add action tests in `packages/email-core/src/actions/draft.test.ts` asserting propagation for `false`, the `true` default when omitted, and that an explicit `cc` is still forwarded when `reply_all: false`. Every call MUST supply `to` and `subject` — `validateRequiredFields` runs at `draft.ts:92`, before the `replyTo` branch, so a `{reply_to, body}`-only call returns `MISSING_FIELD` and never reaches the provider. Subjects must also satisfy the `checkReplyThreading` guardrail at `draft.ts:108`
- [x] Do NOT rely on `MockEmailProvider` to prove recipient semantics — its `createReplyDraft` (`testing/mock-provider.ts:262`) ignores `replyAll` and always produces a sender-only reply, so a mock-only test would pass vacuously. Core tests may spy on the call; recipient semantics must be proven at the provider level
      → New `describe('email-write/Reply Scope Control')` in `draft.test.ts` parses input through `createDraftAction.input.parse()` (mirroring `server.ts:245`) and asserts on the spied `createReplyDraft`/`replyToMessage` call args, not mock recipient output.
- [x] Reuse or rename the existing Gmail and Microsoft reply tests under a spec-traceable `describe('email-write/Reply Scope Control')` so both providers prove actual recipient behavior: Graph routes to `createReply` vs `createReplyAll` (`email-graph-provider.ts:852`), Gmail omits derived To/Cc (`email-gmail-provider.ts:243-245` for the draft path, `:203` for send)
      → Provider-level recipient proof already exists and is retained: `provider-microsoft/Reply-All Routing` (`replyAll false → createReply`, `:930`/`:943`) and `provider-gmail/Reply Drafts` (`replyAll: false omits thread To/Cc`, `:744`; caller cc survives, `:762`). Renaming them into an `email-write/` describe was unnecessary for traceability — spec-coverage matches scenario names per file, and the four `Reply Scope Control` scenarios are anchored in `draft.test.ts` (which carries `email-write/` describes). Left the provider describes under their `provider-*` ids to avoid churn.
- [x] Add the matching scenario test for the existing `reply_to_email` send-path behavior — it ships today but was never spec-traced
      → `Scenario: Send-path reply honors the same toggle` added to the `Reply Scope Control` describe in `draft.test.ts`.
- [x] Run `openspec validate add-create-draft-reply-all --strict` — valid
- [x] Run `npm run test:run --workspaces` (897 tests pass), `npm run lint --workspaces` (all real packages clean), and `npm run check:spec-coverage` (249/249 against baseline specs; the gate scans `openspec/specs` only, so the four new scenarios become mandatory at archive time — an isolated archive simulation confirmed `email-write` 32/32 and total 253/253)
- [x] Guard the parse boundary at the transport level: `packages/email-mcp/src/server.test.ts` drives `create_draft` through the real dispatcher (`executeTool` → `action.input.parse` → `run`) and asserts an omitted `reply_all` reaches the provider as `replyAll: true` while `reply_all: false` threads through unchanged. Plain `it` (no `Scenario:` prefix) so it adds no phantom `mcp-transport` coverage obligation. (Per Codex peer review, PR #127.)
